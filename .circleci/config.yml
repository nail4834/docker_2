# This config was automatically generated from your source code
# Stacks detected: deps:python:.
version: 2.1

orbs:
  python: circleci/python@2

jobs:
  test-python:
    docker:
      - image: cimg/python:3.8-node
    steps:
      - checkout
      - python/install-packages
      - run:
          name: Run tests
          command: |
            set -eo pipefail
            pytest --junitxml=junit.xml || ([ $? -eq 5 ] && echo 'Did not find any tests to run.')
      - store_test_results:
          path: junit.xml

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Deploy
          command: |
            echo "Deploy step goes here"
            # ./deploy.sh  # Uncomment and update with your real deployment script

workflows:
  build-and-test:
    jobs:
      - test-python
      - deploy:
          requires:
            - test-python
